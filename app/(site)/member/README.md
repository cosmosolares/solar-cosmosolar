# Member Area (Level 3)

This directory is reserved for future member-only pages (e.g., profile, settings, exclusive content).

Currently, all routes under `/member` are protected by `middleware.js` and will redirect to `/login` if the user is not authenticated.
